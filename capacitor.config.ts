import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.trimixblender.barefootblender",
  appName: "Barefoot Blender",
  webDir: "dist"
};

export default config;
